const user = require("./user.controller.ts");
const group = require("./group.controller.ts");
const friend = require("./friend.controller.ts");
const expense = require("./expense.controller.ts");

module.exports = {
  user,
  group,
  friend,
  expense,
};
